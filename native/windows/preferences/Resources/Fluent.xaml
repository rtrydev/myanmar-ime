<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Fluent / Win11-style resources for the WPF Preferences app. All
        colors come from DynamicResource Theme.* brushes published by
        Theme.cs at startup and on WM_SETTINGCHANGE. Controls update
        live as light/dark/accent change in Windows Settings.

        Notes for maintainers:
        - WPF has no native "card with rounded corners" primitive, so
          we lean on Border with CornerRadius="8". Same for buttons.
        - Most ControlTemplates are minimal re-skins of the system
          control — we don't try to be WinUI 3, just visually coherent.
        - Hover / pressed feedback uses Theme.Hover (a low-alpha
          neutral) so it works on both light and dark backgrounds
          without needing two separate brushes.
    -->

    <CornerRadius x:Key="Theme.CornerRadius">8</CornerRadius>

    <!-- ============== Text =================================== -->

    <Style x:Key="Theme.PageTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="22"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{DynamicResource Theme.Foreground}"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>

    <Style x:Key="Theme.SectionHeader" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{DynamicResource Theme.Foreground}"/>
        <Setter Property="Margin" Value="0,0,0,2"/>
    </Style>

    <Style x:Key="Theme.Body" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="{DynamicResource Theme.Foreground}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <Style x:Key="Theme.Hint" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{DynamicResource Theme.ForegroundMuted}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <Style x:Key="Theme.MonoCaption" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Consolas, Cascadia Mono, Segoe UI Mono"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{DynamicResource Theme.ForegroundMuted}"/>
    </Style>

    <!-- Default TextBlock styling: nothing in XAML has to opt in. -->
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource Theme.Foreground}"/>
    </Style>

    <!-- ============== Window background ===================== -->

    <!-- A semi-transparent body so Mica shows through when DWM
         honours the backdrop attribute on Win11; on older Windows
         it falls back to the solid Theme.WindowBackground. -->
    <Style x:Key="Theme.WindowChrome" TargetType="Window">
        <Setter Property="Background" Value="{DynamicResource Theme.WindowBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource Theme.Foreground}"/>
        <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

    <!-- ============== Card ================================== -->

    <Style x:Key="Theme.Card" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource Theme.CardBackground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.BorderSubtle}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="{StaticResource Theme.CornerRadius}"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

    <!-- ============== Button ================================ -->

    <Style x:Key="Theme.Button" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource Theme.CardBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource Theme.Foreground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="14,7"/>
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5"
                            SnapsToDevicePixels="True">
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                    Value="{DynamicResource Theme.CardHover}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                    Value="{DynamicResource Theme.Hover}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Default Button styling: nothing in XAML has to opt in. -->
    <Style TargetType="Button" BasedOn="{StaticResource Theme.Button}"/>

    <!-- Accent button — for the primary call-to-action. -->
    <Style x:Key="Theme.AccentButton" TargetType="Button"
           BasedOn="{StaticResource Theme.Button}">
        <Setter Property="Background" Value="{DynamicResource Theme.Accent}"/>
        <Setter Property="Foreground" Value="{DynamicResource Theme.AccentForeground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.Accent}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <Style x:Key="Theme.DestructiveButton" TargetType="Button"
           BasedOn="{StaticResource Theme.Button}">
        <Setter Property="Background" Value="{DynamicResource Theme.CardBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource Theme.Destructive}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.Destructive}"/>
    </Style>

    <Style x:Key="Theme.LinkButton" TargetType="Button"
           BasedOn="{StaticResource Theme.Button}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource Theme.Accent}"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="FontWeight" Value="Normal"/>
    </Style>

    <!-- Small icon-only round button (used as the row delete X). -->
    <Style x:Key="Theme.IconButton" TargetType="Button"
           BasedOn="{StaticResource Theme.Button}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource Theme.ForegroundMuted}"/>
        <Setter Property="Padding" Value="6,2"/>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="MinWidth" Value="24"/>
    </Style>

    <!-- ============== TextBox =============================== -->

    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource Theme.InputBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource Theme.Foreground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,5"/>
        <Setter Property="MinHeight" Value="30"/>
        <Setter Property="CaretBrush" Value="{DynamicResource Theme.Foreground}"/>
        <Setter Property="SelectionBrush" Value="{DynamicResource Theme.Accent}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="{TemplateBinding Padding}"
                                      VerticalAlignment="Center"
                                      Background="Transparent"
                                      Focusable="False"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush"
                                    Value="{DynamicResource Theme.Accent}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============== CheckBox / Toggle ===================== -->

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource Theme.Foreground}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="8,0,0,0"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal"
                                Background="Transparent">
                        <Border x:Name="Box"
                                Width="20" Height="20"
                                Background="{DynamicResource Theme.InputBackground}"
                                BorderBrush="{DynamicResource Theme.Border}"
                                BorderThickness="1"
                                CornerRadius="4"
                                VerticalAlignment="Center"
                                SnapsToDevicePixels="True">
                            <Path x:Name="Check"
                                  Stroke="{DynamicResource Theme.AccentForeground}"
                                  StrokeThickness="2"
                                  Data="M 4 10 L 8 14 L 16 6"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round"
                                  Visibility="Collapsed"/>
                        </Border>
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Box" Property="Background"
                                    Value="{DynamicResource Theme.Accent}"/>
                            <Setter TargetName="Box" Property="BorderBrush"
                                    Value="{DynamicResource Theme.Accent}"/>
                            <Setter TargetName="Check" Property="Visibility"
                                    Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box" Property="BorderBrush"
                                    Value="{DynamicResource Theme.Accent}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============== Slider ================================ -->

    <Style x:Key="SliderRepeatButton" TargetType="RepeatButton">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Background="Transparent" Height="14"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SliderThumb" TargetType="Thumb">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Grid>
                        <Ellipse Width="20" Height="20"
                                 Fill="{DynamicResource Theme.CardBackground}"
                                 Stroke="{DynamicResource Theme.Border}"
                                 StrokeThickness="1"/>
                        <Ellipse Width="10" Height="10"
                                 Fill="{DynamicResource Theme.Accent}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Slider">
        <Setter Property="Foreground" Value="{DynamicResource Theme.Accent}"/>
        <Setter Property="Background" Value="{DynamicResource Theme.BorderSubtle}"/>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid VerticalAlignment="Center">
                        <Border Height="4"
                                CornerRadius="2"
                                Background="{DynamicResource Theme.BorderSubtle}"/>
                        <Track x:Name="PART_Track">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Style="{StaticResource SliderRepeatButton}"
                                              Command="Slider.DecreaseLarge">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Height="4"
                                                    CornerRadius="2"
                                                    Background="{DynamicResource Theme.Accent}"/>
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Style="{StaticResource SliderThumb}"/>
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Style="{StaticResource SliderRepeatButton}"
                                              Command="Slider.IncreaseLarge"/>
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============== TabControl (NavView-ish) ============== -->

    <!-- We render the tab strip as a left-side rail, mimicking
         Settings-app navigation. WPF still routes the content via
         TabControl.SelectedItem, but each TabItem becomes a pill
         button stacked vertically. -->
    <Style x:Key="Theme.NavTabItem" TargetType="TabItem">
        <Setter Property="Foreground" Value="{DynamicResource Theme.Foreground}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="14,9"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6"
                            Margin="0,2"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="Selector"
                                    Grid.Column="0"
                                    Width="3" Height="20"
                                    CornerRadius="2"
                                    Margin="0,0,8,0"
                                    Background="Transparent"/>
                            <ContentPresenter Grid.Column="1"
                                              ContentSource="Header"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                    Value="{DynamicResource Theme.Hover}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                    Value="{DynamicResource Theme.CardBackground}"/>
                            <Setter TargetName="Selector" Property="Background"
                                    Value="{DynamicResource Theme.Accent}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Theme.NavTabControl" TargetType="TabControl">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0"
                                Background="Transparent"
                                Padding="12,16,8,16">
                            <ScrollViewer VerticalScrollBarVisibility="Auto"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel IsItemsHost="True"/>
                            </ScrollViewer>
                        </Border>
                        <Border Grid.Column="1"
                                Style="{StaticResource Theme.Card}"
                                Margin="0,16,16,16"
                                Padding="0">
                            <ContentPresenter ContentSource="SelectedContent"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============== ScrollViewer ========================== -->

    <Style TargetType="ScrollViewer">
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <!-- ============== DataGrid ============================== -->

    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource Theme.Foreground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.BorderSubtle}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource Theme.Divider}"/>
        <Setter Property="RowBackground" Value="Transparent"/>
        <Setter Property="AlternatingRowBackground" Value="Transparent"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="RowHeight" Value="36"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource Theme.ForegroundMuted}"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.Divider}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource Theme.Foreground}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="DataGridRow">
        <Setter Property="Background" Value="Transparent"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background"
                        Value="{DynamicResource Theme.Hover}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ============== Glyph card (Syntax tab) =============== -->

    <Style x:Key="Theme.GlyphCard" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource Theme.CardHover}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.BorderSubtle}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="MinWidth" Value="72"/>
        <Setter Property="MinHeight" Value="64"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

    <Style x:Key="Theme.KeyChip" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource Theme.CardHover}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.BorderSubtle}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Padding" Value="6,2"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

</ResourceDictionary>
