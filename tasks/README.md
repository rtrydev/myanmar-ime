# Open Tasks

Per-task markdown files live in this directory. Each file is a self-contained
work item with a goal, design notes, and an ordered implementation plan.

(no open tasks — see `archive/2026-05-12b/` for the most recent iteration)
